{
  "name": "webshare",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "repository": { "type": "git", "url": "git+https://github.com/freeappstore-online/webshare.git" },
  "scripts": {
    "dev": "pnpm --filter @webshare/web dev",
    "build": "pnpm --filter @webshare/web build",
    "preview": "pnpm --filter @webshare/web preview",
    "typecheck": "pnpm --filter @webshare/web exec tsc -b",
    "test": "pnpm --filter @webshare/web exec tsc -b"
  }
}
